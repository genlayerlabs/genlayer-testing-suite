
networks:
  default: localnet

  localnet:
    url: "http://127.0.0.1:4000/api"
    accounts:
      - "${PRIVATE_KEY1}"

  testnet_asimov:
    url: "http://34.32.169.58:9151"
    accounts:
      - "${PRIVATE_KEY1}"

paths:
  contracts: "contracts"
  tests: "tests"

dotenv: .env
